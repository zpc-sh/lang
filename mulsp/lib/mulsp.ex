defmodule Mulsp do
  @moduledoc """
  mulsp — Micro Universal LSP Proxy

  A crash-resilient, dynamically-partitioned LSP proxy built on AtomVM.
  Each AI agent gets its own mulsp servelet. They cluster via distributed
  Erlang, transfer sparse merkin trees via the DC protocol, and expose
  capabilities over reclaimed dead protocols (gopher, finger, etc).

  mulsp is not the LSP — it's the nervous system that connects AI to the LSP.
  """
end

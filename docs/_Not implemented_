defmodule Elixir.Lang.LSP.Lang.Workspace.Symbol do
  @moduledoc "Workspace symbol search"
  @behaviour Lang.LSP.Handler
  @lsp_method "lang.workspace/symbol"

  @impl true
  def method, do: @lsp_method

  @impl true
  def handle(params, ctx) when is_map(params) and is_map(ctx) do
    # TODO: implement
    {:error, :not_implemented}
  end
end
